---
name: git-policy
description: R5 版本控制策略——只跟踪给人看的文本文件，忽略中间产物
scope: 本仓库（ai-rules）
priority: high
---

# R5 版本控制策略

## 跟踪范围

- 只跟踪**文本文件、给人看的文档**（如 `.md`）
- 不跟踪任何编译产物、二进制文件、缓存、依赖目录

## 忽略维护

- 新出现不需要跟踪的文件类型 → 更新 `.gitignore`
- `.gitignore` 本身**不跟踪**（按约定，配置类文件不属于"给人看的文件"）
- 已知忽略清单：编译产物（build/dist/`*.exe` 等）、缓存（`__pycache__`/`*.log` 等）、依赖目录（`node_modules` 等）

## 已知代价（记录在案）

- `.gitignore` 不随仓库分发：克隆或迁移环境后需重建忽略规则
- 已接受此代价：本目录为个人规范库，无协作者

## 流程衔接

- 修改文件前的 git 检查：见 R3「修改前置检查（git 强制）」
