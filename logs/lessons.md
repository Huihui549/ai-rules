# 踩坑沉淀区

> **状态：持续记录**——记录**跨项目通用**的 AI 不符合预期行为（项目专属的坑写各项目 `pitfalls.md`，R7 归置判断）。
> 同类问题出现 2 次 → 提炼为 `rules/` 下的正式规则，并在末列标注规则编号。
> 已定稿为规则的教训**不在此重复**（SSOT，R7）。

| 日期 | 场景 | AI 的错误行为 | 期望行为 | 沉淀为规则 |
|---|---|---|---|---|
| 2026-08-19 | 备份自启脚本 | 备份文件留在 `/etc/init.d/` 下，被开机脚本按 `S??*` 模式当成启动脚本执行，导致程序双启动 | 备份系统文件必须放在非自动执行目录（如 `/root/`、`/home/`），不得留在 `/etc/init.d/`、cron 等会被自动执行的目录 | 待观察（同类二次后提炼） |
| 2026-09-18 | 脚本写文件传错源变量 | 脚本里把"文档文本"变量当源码传给 `bt_passthrough.c`，整份 markdown 覆盖掉 C 源码（靠临时备份恢复） | 同一脚本内多组"源→目标"必须显式配对命名（如 `$src_c/$dst_c`），写入前校验目标特征串（如 `#include "xxx.h"`）；写前必备份 | 待观察（同类二次后提炼） |

## 已迁出 / 已定稿（不再在此重复）

- 项目专属的坑（时区差 8 小时、GB2312 编码误读）→ 已随 `templates/project-skeleton/pitfalls.md` 示例迁往项目侧
- 输出与决策流程、会话启动、风险检查擅自改动、注释脱节、git 判断、日志可重放、分层文档写法等 → 已定稿为 R1 / R3 / R4 / R5 / R6 / R10 / R11
