# AI 开发流程（用户手册）

> **给人读的说明书**：怎么用 ai-rules、开发一个新功能时按什么顺序走。
> 规则细则在 `rules/`，字段级定义在 R6，本文件不重复（SSOT，R7）；下表与 R6 如有出入，**以 R6 为准**。

## 一、分层分工（七件：谁写、何时变）

| 级别 | 层 | 文件 | 谁写 | 何时变 | 一句话 |
|---|---|---|---|---|---|
| 跨项目 | 行为规则 | `ai-rules/rules/` | 你 + AI | 极少 | 怎么和 AI 协作 |
| 项目级 | 项目上下文 | 根 `AGENTS.md` | 你主写 | 项目演进 | AI 猜不到的硬事实 |
| 项目级 | 项目档案 | `dev-docs/<项目名>/context.md` | AI 填、你审 | 项目演进 | 这个项目是什么 |
| 项目级 | 坑 | `dev-docs/<项目名>/pitfalls.md` | AI 自动追加 | 踩到即写 | 跨功能的坑，别再踩 |
| 项目级 | 决策 | `dev-docs/<项目名>/adr/` | AI 写 | 有取舍时 | 为什么这么定 |
| 功能级 | 规格 | `features/<功能名>/spec.md` | AI 写、你确认 | **冻结一次** | 要什么 + 验收标准 |
| 功能级 | 验证 | `features/<功能名>/verify.md` | AI 起草、你补真机项 | 跟规格走 | 怎么证明做到了 |
| 功能级 | 开发日志 | `features/<功能名>/dev.md` | AI 自动 | 每次改动 | 做了什么、怎么验证 |

**自检判据**：需求变 → 只动 `spec` + `verify`；实现变 → 只动 `dev`；踩坑 → 只动 `pitfalls`；换项目 → 换 `AGENTS.md`/`context.md`，规则底座不动。若一次改动被迫同时改多层里的同一句话，说明分层没分干净。

## 二、目录长什么样

```
<项目根>/
├── AGENTS.md
└── dev-docs/<项目名>/
    ├── context.md
    ├── pitfalls.md
    ├── adr/0001-<主题>.md
    ├── requirements/ design/ ops/
    └── features/<功能名>/
        ├── spec.md
        ├── verify.md
        └── dev.md
```

- **坑与决策放项目级**：它们要被后续功能复用；放功能目录里，别的功能翻不到
- 文档放项目根下的 `dev-docs/`（与代码同仓库同版本）；不用独立文档目录——理由见 R6

## 三、一次性准备（每个新项目）

1. 项目根放 `AGENTS.md`：复制 `templates/project-skeleton/AGENTS.md`，填构建/测试命令、目录职责、禁区、`ai-rules` 路径
   - **同时填运行配置**（AI 会在首次接触时主动向你索要）：交叉编译命令与环境、SSH 是否免密、烧写/启动命令、串口与波特率、日志抓取方式、固件备份位置、只能人工做的步骤
   - 目标：AI 自动跑到"可测状态"（编译 0 warning → 单测/静态检查 → 部署 → 启动 → 抓日志），你只做人工步骤（点屏幕、看现象）并检查结果
2. 建 `dev-docs/<项目名>/`：`context.md`、`pitfalls.md`、`adr/`（按骨架 README 放置）
3. 确认工具会自动读 `AGENTS.md`；不能自动读的，每次贴 `templates/dialogue-opening.md`
   - **验证方法**：新会话问 AI"本项目构建命令是什么"——答不出就没读到，改用粘贴方式，或把 `AGENTS.md` 再复制成工具认的文件名（如 `CLAUDE.md`）
4. 确认项目在 git 仓库内（R4）
5. **之后不必你提醒**：AI 每轮检查这些文件是否需要新建与更新（R6「自动创建与维护」）

## 四、开发一个新功能：五步

| 步 | 你说 | AI 自动做 | 产出 |
|---|---|---|---|
| 1 开工 | "做 X 功能" | 补齐项目级文件若缺；读 `AGENTS.md`/`context.md`/`pitfalls.md`；模糊处先问你；**新建** `features/<功能名>/` 并写 `spec.md` + `verify.md` 草案 | `spec.md`、`verify.md` |
| 2 冻结 | "可以，开始" | 给唯一最优方案 + 改动文件清单 + 每步验证方式 | 计划 |
| 3 实现 | 不用管 | 小步改 → 每步跑 `verify.md` 命令 → 失败自修 → **即时更新** `dev.md` | 代码 + 日志 |
| 4 收尾 | "收尾" | 回填 `dev`/`pitfalls`/`adr`/`verify`，对照验收逐项打勾 | 完整文档 |
| 5 提交 | "提交" | 不做任何 git 写操作（R4）；由你 add/commit | 提交 |

## 五、你只需做三件事

1. **说清目标与验收**——说不清时 AI 会先给你候选选项
2. **拍板**：需求变更、不可逆操作、你自己偏好之间的真冲突、成本或范围明显超出本次任务（即 R1 第三节的 4 类请示情形）
3. **做真机那一半**：上板/联调验证，以及 git 提交

其余（建目录、写文档、选方案、改文件、查一致性、回填教训）由 AI 负责，不该反过来问你。

## 六、常见场景怎么说

| 想要 | 一句话说法 | 行为 |
|---|---|---|
| 学知识 | "讲讲 X" | learner：只给框架，追问才展开 |
| 评估选型 | "A 还是 B，各自风险" | consultant：利弊对照，不动手 |
| 排查问题 | "查 X 为什么失败" | engineer：先给报告与复现，不动手 |
| 只要报告 | "检查一下，别改" | 只报告 + 建议，等你确认（R3） |

## 七、什么时候回来改 ai-rules

- 同一个坑**第 2 次出现且跨项目** → 按 R7 升格为规则（单次的项目坑只留项目侧 `pitfalls.md`）
- 规则之间打架 → 按 R7 冲突裁决表处理
- 每轮改完规则 → 同步 README 规则索引

## 八、升级存量项目（老的单文件 feature.md）

1. 拆成三件：`spec.md`（原"需求/验收"段）、`verify.md`（散落的测试项收成命令与清单）、`dev.md`（实现过程按功能单元重排）
2. 老文档里的坑与决策上提到项目级：`pitfalls.md`、`adr/`
3. 项目根补 `AGENTS.md`：命令、目录职责、禁区可从旧文档与过往踩坑里抽
4. 拆分完的旧文档移入 `features/<功能名>/archive/` 归档，或删掉（内容已迁出）
5. 此后一律按本手册第四节走
