# <功能名> 开发日志（dev.md，可重放）

> 放在 `dev-docs/<项目名>/features/<功能名>/dev.md`，AI 每完成一个可验证步骤**即时**追加（R6）。
> 按功能单元分节（依赖序）；单会话多个小功能不得合并成一段笼统叙述；只写结论与事实，不写流水账。
> 引用坑与决策用编号：`坑 #3`、`ADR-0002`（定义在项目级 `pitfalls.md` / `adr/`）。

## 单元 1：<名称>

- **目标**：
- **改动**：`<文件>` → <行为变化>
- **事实**：<协议 / 数据 / 接口 / 时序的关键事实>
- **验证**：`<命令>` → <结果>
- **依赖**：<前置单元 / 外部条件 / 相关坑与决策编号>

## 单元 2：<名称>

- **目标**：
- **改动**：
- **事实**：
- **验证**：
- **依赖**：

## 遗留

- <未完成项 / 待观察项>
