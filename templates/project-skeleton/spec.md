# <功能名> 规格（spec.md）

> 放在 `dev-docs/<项目名>/features/<功能名>/spec.md`，AI 开工时自动创建；填写后由用户确认并**冻结**需求与验收标准（R6），此后任何改动都要显式说明。
> 同目录配套：`verify.md`（可执行验证）、`dev.md`（开发日志）；坑与决策在**项目级** `dev-docs/<项目名>/pitfalls.md`、`adr/`。

## 任务

<一句话：要做什么>

## 输入

<触发条件 / 数据来源 / 已知信息>

## 处理

<要做什么：步骤或规则，越具体越好>

## 输出

<期望结果 / 交付物 / 文件>

## 性能要求（必须量化）

- <如按键响应时间 < 50ms；帧周期 ≤ 20ms；CPU 占用 < 60%>
- 无性能敏感要求时写"无"；测量方式见项目级 `AGENTS.md`「性能红线」

## 边界与异常

<超时、错误输入、特殊情况如何处理>

## 约束

<技术栈、禁止事项、性能要求、风格要求>

## 验收清单（冻结）

- [ ] <逐条可勾选，AI 完成后自查、你复核；可执行命令类同步进 verify.md>
- [ ]
