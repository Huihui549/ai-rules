# <项目名> 踩坑记录（pitfalls.md）

> **项目级·跨功能**：本项目的坑都记这里（不是每个功能一份）；AI 踩到即自动追加（R6）。
> 通用到跨项目的约束才写 `ai-rules/rules/`（R7）。遇到同类问题先查本表。

| # | 日期 | 现象 | 根因 | 正确做法 | 位置 | 来源功能 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-25 | 开发板 Qt 显示时间比 PC 差 **整 8 小时** | 设备时区是 UTC | 先查设备：`date`、`ls -l /etc/localtime`；修复 `ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime`，不必改 Qt 代码 | [开发板] | <功能名> |
| 2 | 2026-09-14 | 改写 GB2312 源文件后中文注释全变乱码 | 误按 UTF-8 读写 | 改写前先验证编码（非 UTF-8 按 GBK 处理）；脚本产物走"数据文件 + 纯 ASCII 脚本"写回原编码 | [PC] | <功能名> |

> 以上两行是**示例**（从 ai-rules 迁来的真实教训），复制到项目后按本项目实际情况替换。
> 编号稳定不重排：`dev.md` 里用「坑 #1」引用。
