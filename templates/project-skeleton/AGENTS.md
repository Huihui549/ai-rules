# <项目名> — AI 协作须知（AGENTS.md）

> 复制到项目根目录，改名 `AGENTS.md`。只写 **AI 猜不到的硬事实**（命令、路径职责、硬约定、禁区）。
> 不写通用规范——通用行为规则在 `ai-rules/`；写超过一屏就是写多了。

## 协作规则

- 行为规则见 `ai-rules/rules/`，路径：<填，如 `D:\...\ai-rules` 或共享盘 `\\192.168.x.x\...\ai-rules`>
- 该库不可见时，先让用户粘贴其 `templates/dialogue-opening.md` 规则段再开工
- 项目文档的位置与维护方式见 `ai-rules/rules/R6-feature-docs.md`

## 构建 / 运行 / 测试

| 用途 | 命令 | 备注 |
|---|---|---|
| 构建（交叉编译） | `<command>` | 环境变量 / 工具链路径： |
| 单元测试 | `<command>` | |
| 静态检查 | `<command>` | 门槛：`-Wall -Wextra -Werror` + cppcheck（可选 MISRA 子集） |
| 烧写 / 部署 | `<command>` | [开发板] |
| 启动 / 重启程序 | `<command>` | [开发板] |

## 日志规范（让 AI 能自动判读）

| 项 | 约定 |
|---|---|
| 分级 | ERROR / WARN / INFO / DEBUG，编译期可关 |
| 格式 | `[时间戳][模块][级别] 内容`，一条一行 |
| 输出 | 串口 + 重定向到文件：<[开发板] 路径> |
| 固件版本 | 构建时写入 `git describe`，启动首行打印 —— 看到日志即知板上版本 |

## 性能红线（本项目热路径禁区 + 测量手段）

| 项 | 内容 |
|---|---|
| 性能指标 | <如按键响应 <50ms、帧周期 ≤20ms、CPU 占用 <60%> |
| 热路径禁区 | <如禁止动态分配、禁止逐帧 memcpy、禁止热路径打日志、禁止阻塞延时> |
| 测量手段 | <DWT 周期计数 / GPIO 翻转 + 示波器 / 串口打点> |
| 测量入口 | <如 `#define PROF_ENABLE 1` 打开打点> |

## 远程运行与自动化边界（缺项请主动向用户索要，勿猜测）

| 项 | 值 |
|---|---|
| SSH 方式（是否免密） | <`ssh user@host`，免密=是/否；否则只能由用户执行> |
| 串口设备与波特率 | <[PC] `COM3` 115200 / [开发板] `/dev/ttyS0`> |
| 日志抓取方式 | <如 `ssh ... 'tail -f /tmp/app.log'` 或串口重定向到文件> |
| 固件备份位置与命令 | <烧写前备份当前固件> |
| 只能人工做的步骤 | <如登录、点屏幕、看现象> |

- **自动化目标**：AI 自动跑到"可测状态"（编译 0 warning → 单测/静态检查通过 → 部署 → 启动 → 抓日志），用户只做人工步骤并检查结果
- 命令**只在这里定义一次**；`features/<功能名>/verify.md` 只列本功能要跑哪些、通过标准是什么（SSOT，R7）

## 目录职责

| 路径 | 职责 |
|---|---|
| `<src/>` | |
| `<drivers/>` | |

## 硬约定

- 命名：
- 风格：
- 禁止：
- 必须：

## 禁区（踩过的坑，勿再犯）

- <一条一句，写清"不要做什么"与"为什么">

## 本项目文档位置

- 规格 / 日志 / 验收 / 坑 / 决策：`dev-docs/<项目名>/features/<功能名>/`（R6）
