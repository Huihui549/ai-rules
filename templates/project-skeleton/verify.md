# <功能名> 验收清单（verify.md）

> 放在 `dev-docs/<项目名>/features/<功能名>/verify.md`，**开工时与 `spec.md` 一起建**（R6）。
> 上半是**可执行检查**（AI 每步自跑，失败自修）；下半是**真机项**（用户执行）；收尾逐项打勾。
> **命令只在项目根 `AGENTS.md` 定义一次**，本表只写"本功能要跑哪些 + 通过标准"（SSOT，R7）。

## 本功能自动检查（AI 执行）

| 检查 | 命令（见 AGENTS.md） | 通过标准 | 结果 |
|---|---|---|---|
| 构建 | 构建命令 | 0 error / 0 warning | |
| 单元测试 | 测试命令 | 全绿 | |
| 静态检查 | 检查命令 | 无新增告警 | |
| 本功能专项 | <如跑某脚本 / 抓串口日志核对> | | |
| 性能 | 见 AGENTS.md「性能红线」测量手段 | 达到 spec 里的量化指标（附实测数据） | |

## 回归项（修过的 bug，防复发）

| 来源 bug | 检查方式（命令或真机步骤） | 通过标准 |
|---|---|---|
| <现象一句话> | `<command>` 或 <上板步骤> | |

## 真机验收（用户执行）

- [ ] <上板 / 联调步骤> → 期望现象：<...>
- [ ] <异常场景> → 期望行为：<...>

## 收尾

- [ ] 规格中的每条验收项都已核对
- [ ] `dev.md` 已按功能单元补齐
- [ ] 新坑已写入项目级 `pitfalls.md`；决策已写入项目级 `adr/`
